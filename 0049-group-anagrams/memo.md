### step1

strsの文字列をfor文で回して、sortしたものをキーとしたマップに元の文字列をvectorで入れていって、最後にそのvectorをreturnするという方針で解きました。

### step2

Naoto Iwaseさんのコードを参考に、キーをソートした文字列ではなくてa-zまでの出現頻度を表す文字列に変換したものを使って解きなおしました。

### step3

step1のコードを3回通すまで書き直し。